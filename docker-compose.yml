version: '3.8'

services:
  projectxia:
    build: .
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
      - JWT_SECRET=projectxia_super_secret_jwt_key_prod_2026
      - RAZORPAY_KEY_ID=rzp_test_projectxia_live
      - RAZORPAY_KEY_SECRET=projectxia_secret_key_mock
    restart: always
